version: '3'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./ota-update/data:/usr/share/nginx/html/shunt
      - ./ota-update/nginx.conf:/etc/nginx/conf.d/default.conf